# example-react-native-crashlytics

Minimal repro for iOS dsym issue.

In the root project folder:
```
npm ci
npx pod-install
```

Build and run on the iOS device. 

Click on the crash button and relaunch the application.